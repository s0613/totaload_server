spring:
  profiles:
    active: local

# SpringDoc ??
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true




management:
  endpoints:  # endpoint? ??? endpoints
    web:
      base-path: /actuator
      exposure:
        include: health,info  # ??? ????? ??
  endpoint:
    health:
      show-details: when-authorized  # ???? ?? ?? ?? ??
  health:
    db:
      enabled: true

